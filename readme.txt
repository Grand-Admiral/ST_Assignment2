Setup:
>to setup this program you must have all the python files in a localised folder.
>next all the excel files must be placed inside a folder called data. 
>>now the main folder should hold all the python programs plus the data folder holding the excel files

run the programs and see the data form.
